spring:
  jpa:
    show-sql: true
    database: h2
    hibernate:
      ddl-auto: create
    open-in-view: false
management:
  endpoints:
    web:
      exposure:
        include:
          - "*"

logging:
  level:
    root: info
